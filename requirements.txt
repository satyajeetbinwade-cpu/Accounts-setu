pandas
pyyaml
rapidfuzz
openpyxl
cryptography
pymupdf
reflex==0.9.12
pytest
# PDF export prints the report with headless Chromium via Playwright.
# Deliberately NOT wkhtmltopdf/QtWebKit, which silently collapses the report's
# CSS Grid layout to one column and inverts the colour theme.
# After installing, run: playwright install chromium
playwright
