"""C/M8 — Action Center package (Reconciliation Exceptions only, this phase).

The single working queue staff act from, built GENERICALLY against the
shared Flagged Item interface (Step 3's Information Architecture) so
Modules 1/4/5/6 slot in later without a rebuild.
"""